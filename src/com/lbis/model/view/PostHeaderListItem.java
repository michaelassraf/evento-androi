package com.lbis.model.view;

public interface PostHeaderListItem extends ListItemAbs {

	// public PostHeaderListItem(Context ctx , Post post){
	// setMainText(post.getPostTitle());
	// setSubText(post.getPostPoster());
	// setPicturePath(Utils.getInstance().getUserProfile(ctx,
	// post.getPostPosterId()));
	// }

}
